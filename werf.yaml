configVersion: 1
project: myproj
---
image: one
from: ubuntu:18.04
fromCacheVersion: "2"
git:
- add: /.helm
  to: /app
  stageDependencies:
    install:
    - templates/templates.yaml
shell:
  install:
  - apt update && apt install -q -y wget
  - wget https://github.com/werf/werf/archive/v1.1.22+fix31.zip
